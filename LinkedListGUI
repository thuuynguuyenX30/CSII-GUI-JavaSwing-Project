import javax.swing.*;

public class LinkedListGUI<T> {
    private JFrame frame;
    private JButton addButton, removeButton, clearButton;
    private JTextField textField;

    public LinkedListGUI() {
        initComponents();
    }

    private void initComponents() {
        frame = new JFrame("LinkedList GUI");
        addButton = new JButton("Add");
        removeButton = new JButton("Remove");
        clearButton = new JButton("Clear");
        textField = new JTextField(20);

        // Set the layout manager, add the components to the frame, etc.
        // We need to add our components to the frame and set the frame visible.
    }

    private void addActionListeners(LinkedList<T> list) {
        addButton.addActionListener(e -> {
            // Parse the text to the appropriate type before adding it to the list
            T value = parseText(textField.getText());
            list.add(value);
        });

        removeButton.addActionListener(e -> {
            // Parse the text to the appropriate type before removing it from the list
            T value = parseText(textField.getText());
            list.remove(value);
        });

        clearButton.addActionListener(e -> {
            list.clear();
        });
    }

    private T parseText(String text) {
        // Parse the text to the appropriate type and return it
        // This will depend on what T is
    }
}
